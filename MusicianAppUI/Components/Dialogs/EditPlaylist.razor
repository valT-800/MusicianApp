<MudDialog>
    <DialogContent>
        <MudTextField T="string" @bind-Value="@Playlist.Name" Label="Name"></MudTextField>
        <MudSelect T="string" @bind-Value="@Playlist.Description" Label="Favorite Coffee" AnchorOrigin="Origin.BottomCenter">
            <MudSelectItem Value="@Playlist.Description"/>
            <MudSelectItem Value="@("Cafe Latte")"/>
            <MudSelectItem Value="@("Espresso")"/>
            <MudSelectItem Value="@("Irish Coffee")"/>
        </MudSelect>
    </DialogContent>
    <DialogActions>
        <MudButton OnClick="Cancel">Cancel</MudButton>
        <MudButton Color="Color.Primary" OnClick="Submit">Ok</MudButton>

    </DialogActions>
</MudDialog>
@code {
    
    [CascadingParameter] MudDialogInstance? MudDialog { get; set; }

    [Parameter] public PlaylistModel Playlist { get; set; } = new();
    
    void Submit() => MudDialog?.Close(DialogResult.Ok(Playlist));
    
    void Cancel() => MudDialog?.Cancel();
}